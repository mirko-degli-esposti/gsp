# Part II — Sources and their certification
## Draft v0.1 — §II.4 (20 August 2026)

> Complements `report_part2_v0.1.md` (§II.1–II.3, II.5). Table II.2a is
> generated by `scripts/diagnostica/tabella_fonti.py` from the registry and
> the tag→ring map in `fonti/anelli.yaml`; this section adds the third leg,
> the columns. Column names are those of Appendix A / riferimento v24.
> **[v]** cross-check the column lists against Appendix A when it is
> assembled.

---

### II.4 From source to ring to column

Every column of a population file can be traced to the registry entries
that determine it, through the ring that computed it. The table below runs
the three legs together, by column group; the per-entry detail (universe,
vintage, licence) is in Table II.2a, and the mechanism in Part I. Columns
are named as in the released Parquet where they survive the public regime,
and marked ° where they exist only in the `persona`/`narrativo` regimes
(§I.7).

| columns | ring | determined by (registry ids) |
|---|---|---|
| `zona`, `sesso`, `eta`, `stato_civile`, `cittadinanza`, `istruzione`, `condizione`, `background`, `origine_genitori` | 1 | the eleven SDMX tables: `istat_anag_sesso_eta_statociv` (hard margin, 1 Jan), `istat_cens_*` (soft, 31 Dec N−1); zone tables from `istat_cens_sesso_eta_cittadinanza` and, for Bologna, `bologna_cittadinanza_zone` (zone–quartiere hierarchy) |
| the 21 AVQ variables (`SALUTE`, `AMBIENTE`, `FIDUCIA`, `PUNTIFI1–13`, `FIDMED`, `FIDINF`, `BMI`, …) and the donor signature | 2 | `avq_microdati` (pools: Emilia-Romagna 4,629 / Lombardia 8,111), decoded via `avq_tracciato_2024` |
| `paese`, `area` (country of citizenship) | 2 | `istat_cens_stranieri_paesi` (municipal margin, tier 0) refined by the sub-municipal sources where they exist: `bologna_cittadinanza_zone` (tier 2), `brescia_cittadinanza_quartieri`, `forli_cittadinanza_quartieri`, `ravenna_cittadinanza_aree`, `reggio_cittadinanza_circoscrizioni` (tier 1–2), `parma_microdati_residenti` (tier 3, section level) |
| `sezione` | 3 | `istat_sezioni_2023` (counts), decoded via `istat_sezioni_2023_tracciato` |
| `eta_anni` | 3 | `istat_sezioni_2023` (single-year section columns `P30+k`/`P67+k`) |
| `lon`, `lat` (public regime: random within section); `via`°, `civico`° | 3 | `anncsu_indirizzario` joined to section geometries `istat_sezioni_shp` |
| `id_nucleo`, `ruolo` (separate file `nuclei_{comune}.csv`) | 4 | `repertorio_nuclei_v1` (configurations, derived from `avq_microdati`), size margins PF3–PF8 from `istat_sezioni_2023`, same-sex couple rates examined against `istat_unioni_civili_2023` |
| `settore`, `posizione`° | D | `cens2011_caratt_attl` (joint sector × position of the occupied, 2011 structure) |
| `titolo_studio`° (detailed title) | D | `cens2011_titolo_studio` (title tree) ordered and currentised by `claist_2026` |
| `nome`°, `cognome`° | D | `firenze_cognomi_2013` (Italian surnames; 2012 edition as stability check only), `modena_nomi_residenti` (first names by sex), `popular_names_nomi`/`_cognomi` (foreign), `cognomi_wiki_MA_ARAB`/`_NG_YORUBA` (specific communities) |
| national reference ticks (viewer only, not a population column) | W | `avq_medie_nazionali`, derived from `avq_microdati` |

Three remarks the table compresses.

*The tier structure of the country of citizenship is visible as a column's
provenance changing by municipality.* The same two columns (`paese`,
`area`) are determined in Ferrara, Modena, Rimini, Piacenza and Castenaso by
the municipal census margin alone (tier 0), in Brescia, Reggio, Ravenna and
Forlì by a sub-municipal open-data table over the census margin (tier 1),
in Bologna by the zone table that also fixes the zone–quartiere hierarchy
(tier 2), and in Parma by the municipal register extract at section level
(tier 3). The registry records this as five sources carrying the same
`usabile_per` tags at different `geografia_paese_tier*`, and the per-
municipality tier is printed by the generation log (§III.2 table). Their
vintages differ — Reggio's table is from 2013, thirteen years older than
the rest, the only sub-municipal source available for that municipality;
the tiers use these tables for the *composition* of the foreign population,
while every total comes from the census margin, which bounds but does not
cancel the staleness. Declared here and in the entry.

*Ring-4 columns live in a separate file by design.* `id_nucleo` and `ruolo`
are written to `nuclei_{comune}.csv`, never into the population file: the
population is read-only once generated (§I.1), and consumers join on `uid`.
The repertoire that shapes the nuclei is itself a registered derived source
with `derivato_da: avq_microdati`, so its provenance chain closes on the
same microdata as ring 2.

*Derived columns cite structure, not information.* The 2011 census tables
and CLAIST determine how a ring-1/2 state is *rendered* (which detailed
title, which sector) but add no information beyond it; the onomastic
repertoires determine names that are plausible and therefore collident. The
public regime drops precisely the columns of this group (°): what
individuates is derived, and what is derived can be omitted without
analytic loss.

---

### Open items for §II.4

1. **[v]** Cross-check column names and the ° marks against Appendix A when
   assembled from riferimento v24 §1–2.
2. **[v]** `usato_da` in several registry entries cites superseded script
   names (`build_constraints.py`, `opendata_paese.py`, `assign_nationality.py`);
   align with the current chain (`cs_build.py`, `enrich.py`) in a registry
   patch — cosmetic for the pipeline, visible in Table II.2a.

